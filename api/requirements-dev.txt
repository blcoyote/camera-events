# Development and test dependencies for the camera-events API.
# Install alongside the production requirements:
#   pip install -r requirements.txt -r requirements-dev.txt

# ── Test runner ─────────────────────────────────────────────────────────────
pytest==8.*
pytest-asyncio==0.23.*
pytest-mock==3.*

# ── HTTP test client (ASGI support) ─────────────────────────────────────────
httpx==0.27.*

# ── Container-backed integration fixtures ───────────────────────────────────
testcontainers[redis,minio]==4.*

# ── HTTP mocking for unit tests that call external services ─────────────────
requests-mock==1.*
responses==0.25.*

# ── Time manipulation ────────────────────────────────────────────────────────
freezegun==1.*

# ── Code quality ─────────────────────────────────────────────────────────────
ruff==0.4.*
